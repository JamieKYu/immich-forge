# Test-only dependencies. Layered on top of requirements.txt (core runtime).
# The ML stack (requirements-ml.txt: torch/basicsr/realesrgan/gfpgan) is NOT
# needed — the code paths under test all run on the classical-CV fallbacks, so
# CI stays GPU-free and fast.
pytest==8.3.4
pytest-asyncio==0.25.2
respx==0.22.0
